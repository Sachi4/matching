# Supabaseプロジェクトの URL / anonキー（Project Settings > API）
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 以下はEdge Function（supabase secrets set で設定する。.envには書かない）
# OPENAI_API_KEY=      embedding生成用（未設定時は簡易フォールバックで動作）
# ANTHROPIC_API_KEY=   反応名生成用（未設定時は簡易フォールバックで動作）
