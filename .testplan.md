# E2E Test Plan — 運命の出会いを科学する MVP (PR #1, branch devin/1786768328-mvp)

Env (already running): Next dev http://localhost:3000, local Supabase :54321 (db reset with committed migration 8aae838, seed = 3 stimuli), `functions serve` running (fallback embedding path — no OPENAI/ANTHROPIC keys). DB clean (0 participants/responses/matches).

Grounding: src/app/page.tsx (register), diagnosis/page.tsx, respond/page.tsx (MatchPopup L204), feed/page.tsx + components/CollectiveCanvas.tsx + ResonanceFeed.tsx (realtime channels), screen/page.tsx, admin/stimuli/page.tsx (safeImageUrl), supabase/functions/submit-response/index.ts (fallbackPhrase L107: first 4 non-punct chars of each text joined by 「と」), migration RPC L86-143 (identical text + identical diagnosis ⇒ resonance = 0.6*1 + 0.4*1 = 1.0 > 0.8 ⇒ match without threshold change).

## T1: Registration inserts participant and navigates to /diagnosis
1. Go to http://localhost:3000/, type nickname `ほしぞら`, click はじめる.
- PASS: navigates to /diagnosis showing question 1/6; SQL `select nickname from participants` returns ほしぞら.

## T2: Diagnosis 6 questions → result card + diagnosis_scores row
2. Answer all 6 questions picking the FIRST option each time.
- PASS: after Q6 a result card appears with a type key, type name, gradient border, and 3 axis values (ひらく/ゆらぎ/ひらめき); SQL shows 1 row in diagnosis_scores for ほしぞら with matching type_key.
3. Click 画像を見にいく → /respond.

## T3: /respond shows 3 seeded images; submit inserts response with embedding
4. Image 1/3 (/stimuli/01.svg) visible. Select tags しずか + つめたい, type free text `夜のプールに沈んでいくみたい`, click この感覚を記録する.
- PASS: advances to 2/3 with different image; SQL: stimulus_responses has 1 row, response_text = `しずか、つめたい。夜のプールに沈んでいくみたい`, embedding IS NOT NULL.
5. Image 2: select tag あたたかい only, submit. Image 3: free text only `ざわざわする`, submit.
- PASS: completion screen 「すべての画像に答えました」+ 共鳴フィードを見る button; SQL count = 3 responses, all with embeddings; no matches yet.

## T4: /feed baseline (participant A)
6. Click 共鳴フィードを見る → /feed.
- PASS: canvas shows colored blobs (NOT blank — regression check for StrictMode fix); feed section shows 「まだ共鳴はありません」.

## T5: Match flow + realtime (2nd participant, incognito, side-by-side windows)
7. Arrange A's /feed window on left half, open incognito window on right half at localhost:3000.
8. In incognito: register `つきかげ`, answer all 6 diagnosis questions with the SAME (first) options, go to /respond, image 1: select しずか + つめたい, free text `夜のプールに沈んでいくみたい` (identical), submit.
- PASS (popup): MatchPopup appears at bottom: 「共鳴が生まれました！」 with reaction phrase 「しずかつとしずかつ」 (fallbackPhrase = first 4 non-punct chars of both texts joined with と).
- PASS (realtime, no reload): A's /feed window on the left updates WITHOUT reload: a new blob appears on canvas AND a match card pops in showing 「しずかつとしずかつ」, ほしぞら × つきかげ, 共鳴度 100%.
- PASS (DB): matches table has 1 row, score = 1.0 (±0.001), reaction_phrase not null.

## T6: /screen big-screen view
9. Navigate a maximized window to /screen.
- PASS: full-screen canvas background with blobs + title 「感じ方の共鳴、リアルタイム」(per screen/page.tsx) + large feed card with the match visible.

## T7: /admin/stimuli add / invalid URL / toggle / delete, reflected in /respond
10. Go to /admin/stimuli. List shows 3 seeded stimuli with thumbnails.
11. Add: URL `/stimuli/02.svg`, label `テスト追加`, click 追加.
- PASS: 4th row appears with 表示中 badge.
12. Type `javascript:alert(1)` in URL field, click 追加.
- PASS: error message shown, no row added (still 4 rows).
13. Toggle stimulus #1 (ゆらぎ) to 非表示; open /respond in incognito (participant つきかげ, answered only image 1).
- PASS: /respond now serves active stimuli only — つきかげ sees remaining count consistent with 3 active stimuli (02,03,テスト追加) and first unanswered is 02 image at position 1/3... concretely: header shows `1 / 3` (not /4) and image is NOT 01.svg's palette. (つきかげ answered 01 which is now inactive, so unanswered = 02, 03, 追加分).
14. Delete the added `テスト追加` row (confirm dialog → OK).
- PASS: row disappears (3 rows); re-toggle ゆらぎ back to 表示中 for cleanliness.

## T8: Mobile viewport sanity (quick)
15. Use Chrome device toolbar (or narrow window ~390px) on / and /respond.
- PASS: layout intact — centered column, no horizontal overflow, buttons full width.

Cleanup/notes: record T1–T7 (T8 optional in recording); no threshold lowering needed (identical inputs ⇒ resonance 1.0).
